<!DOCTYPE html>
<html>
<%- include('./partials/head') %>

  <body>

    <%- include('./partials/nav') %>
        <!-- Page content -->
        <div class="ps-content fullPage">

          <!-- Menu Section -->
          <div class="ps-row ps-padding-64" id="menu">
            <div class="ps-col l6 ps-padding-large">

              <h1 class="ps-center">
                <%= plato.titulo %>
              </h1>

              <br />

              <h4>
                <%= plato.descripcionDetallada==='Plato Típico' ? plato.descripcion : plato.descripcionDetallada %>
              </h4>

              <!--  <h4>
                <% if (plato.descripcionDetallada ==='Plato Típico' ) { %>
                  <%= plato.descripcion %>
                    <% } else { %>
                  <%= plato.descripcionDetallada %> 
                <% } %>
              </h4> -->

              <p class="ps-text-grey">
                U$S <%= plato.precio %>
              </p>

            </div>

            <div class="ps-col l6 ps-padding-large ps-img-menu">
              <img src="/images/<%= plato.imagen  %> " class="ps-round ps-image ps-opacity-min" alt="Menu" />
            </div>
          </div>

          <%- include('./partials/footer',{ clase:'bottom'}) %>

        </div>
  </body>

</html>